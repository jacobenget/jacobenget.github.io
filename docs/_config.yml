title: Jacob Enget's Personal Website
collections:
  projects:
    output: true
    permalink: /:collection/:slug
plugins:
  - jekyll-redirect-from # Enables the use of this plugin, https://github.com/jekyll/jekyll-redirect-from, which enables the Jekyll 'redirect_from' feature
